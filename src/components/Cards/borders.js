import React from 'react';
import { Grid, Image } from 'semantic-ui-react'



const borders = () => {
    const bordersNumber = borders.map(b)
    return (
        <Grid.row>

        </Grid.row>
    )
}